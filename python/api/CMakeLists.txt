option(SPATIAL_PYTHON_BINDINGS "Build pybind11 bindings" OFF)
if(SPATIAL_PYTHON_BINDINGS)
  find_package(pybind11 REQUIRED)
  pybind11_add_module(spatial_py src/module.cpp)
  target_link_libraries(spatial_py PRIVATE spatial_cad spatial_core)
else()
  add_library(spatial_py_stub INTERFACE)
  target_include_directories(spatial_py_stub INTERFACE include)
endif()
